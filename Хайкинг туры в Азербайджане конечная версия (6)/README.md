# 🏔️ TeamTour: Хайкинг туры в Азербайджане

**TeamTour** — это современное веб-приложение для поиска и бронирования пеших горных туров по Азербайджану. Проект разработан с использованием **React**, **TypeScript** и **Supabase** для обеспечения высокой производительности и надежности.

## 🚀 Особенности проекта

* **Каталог туров:** Динамический список маршрутов с фильтрацией.
* **Мультиязычность:** Поддержка нескольких языков (реализовано через `locales`).
* **Современный UI:** Адаптивный интерфейс для мобильных и десктопных устройств.
* **Бэкенд на Supabase:** Аутентификация пользователей и хранение данных о турах.
* **Безопасность:** Использование строгой типизации TypeScript.

## 🛠️ Технический стек

### Клиентская часть (Frontend)
* **React** — библиотека для построения интерфейсов.
* **TypeScript** — для статической типизации (`types.ts`, `.tsx`).
* **Vite** (предположительно) — сборщик проекта (судя по `main.tsx`).
* **CSS / Styles** — кастомная стилизация компонентов.

### Серверная часть и База данных (Backend)
* **Supabase** — открытая альтернатива Firebase.
    * Аутентификация.
    * PostgreSQL база данных.
    * Edge Functions (`supabase/functions/server`).

## 📂 Структура проекта

```text
src/
├── components/          # Переиспользуемые React-компоненты (UI)
├── context/             # React Context (глобальное состояние приложения)
├── data/                # Статические данные или моки
├── guidelines/          # Гайдлайны проекта и правила разработки
├── locales/             # Файлы переводов (i18n)
├── styles/              # CSS модули и стили
├── supabase/            # Логика работы с Supabase
│   └── functions/       # Серверные функции (Edge Functions)
├── utils/               # Утилиты и вспомогательные функции
│   └── supabase/        # Клиент инициализации Supabase
├── App.tsx              # Корневой компонент приложения
├── main.tsx             # Точка входа (рендеринг React DOM)
├── types.ts             # TypeScript интерфейсы и типы
└── index.css            # Глобальные стили
